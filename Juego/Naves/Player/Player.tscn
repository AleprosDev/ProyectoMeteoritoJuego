[gd_scene load_steps=16 format=2]

[ext_resource path="res://Juego/Naves/Player/nave.png" type="Texture" id=1]
[ext_resource path="res://Juego/Naves/Player/MaterialFisicol.tres" type="PhysicsMaterial" id=2]
[ext_resource path="res://Juego/Naves/Player/Player.gd" type="Script" id=3]
[ext_resource path="res://Juego/Naves/Player/CamaraPlayer.tscn" type="PackedScene" id=4]
[ext_resource path="res://Juego/Proyectiles/Laser/LaserBeam2D.tscn" type="PackedScene" id=5]
[ext_resource path="res://Juego/Naves/Player/Estela/EstelaPuntoInicio.tscn" type="PackedScene" id=6]
[ext_resource path="res://Juego/Naves/NaveBase.tscn" type="PackedScene" id=7]
[ext_resource path="res://Juego/Naves/Player/Motor/MotorSFX.tscn" type="PackedScene" id=8]
[ext_resource path="res://Juego/Naves/Player/AnimacionesPlayer.tscn" type="PackedScene" id=9]
[ext_resource path="res://Juego/Naves/Player/Escudo/Escudo.tscn" type="PackedScene" id=10]
[ext_resource path="res://Juego/Proyectiles/ProyectilPlayer.tscn" type="PackedScene" id=11]
[ext_resource path="res://Juego/Naves/Player/impacto.ogg" type="AudioStream" id=12]

[sub_resource type="Shader" id=2]
code = "shader_type canvas_item;

uniform vec4 color : hint_color = vec4(1.0);
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;
uniform bool add_margins = true; // only useful when inside is false

void vertex() {
	if (add_margins) {
		VERTEX += (UV * 2.0 - 1.0) * width;
	}
}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;
		
		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}
		
		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);
			
			if ((xy != clamp(xy, vec2(0.0), vec2(1.0)) || texture(texture, xy).a == 0.0) == inside) {
				return true;
			}
		}
	}
	
	return false;
}

void fragment() {
	vec2 uv = UV;
	
	if (add_margins) {
		vec2 texture_pixel_size = vec2(1.0) / (vec2(1.0) / TEXTURE_PIXEL_SIZE + vec2(width * 2.0));
		
		uv = (uv - texture_pixel_size * width) * TEXTURE_PIXEL_SIZE / texture_pixel_size;
		
		if (uv != clamp(uv, vec2(0.0), vec2(1.0))) {
			COLOR.a = 0.0;
		} else {
			COLOR = texture(TEXTURE, uv);
		}
	} else {
		COLOR = texture(TEXTURE, uv);
	}
	
	if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, TEXTURE)) {
		COLOR.rgb = inside ? mix(COLOR.rgb, color.rgb, color.a) : color.rgb;
		COLOR.a += (1.0 - COLOR.a) * color.a;
	}
}"

[sub_resource type="ShaderMaterial" id=3]
shader = SubResource( 2 )
shader_param/color = Color( 0.85098, 0.631373, 0.207843, 1 )
shader_param/width = 3.0
shader_param/pattern = 0
shader_param/inside = false
shader_param/add_margins = true

[sub_resource type="CircleShape2D" id=1]
radius = 39.5414

[node name="Player" instance=ExtResource( 7 )]
z_index = 50
z_as_relative = false
collision_layer = 1
collision_mask = 22
physics_material_override = ExtResource( 2 )
contacts_reported = 2
contact_monitor = true
can_sleep = false
linear_damp = 1.5
angular_damp = 4.5
script = ExtResource( 3 )
hitpoints = 500.0
num_explosiones = 3
potencia_motor = 20

[node name="Sprite" parent="." index="0"]
material = SubResource( 3 )
rotation = 1.5708
texture = ExtResource( 1 )

[node name="CollisionShape2D" parent="." index="1"]
shape = SubResource( 1 )

[node name="Canion" parent="." index="2"]
proyectil = ExtResource( 11 )

[node name="PuntoIzquierdo" parent="Canion" index="0"]
position = Vector2( 0.12373, -36 )

[node name="PuntoDerecho" parent="Canion" index="1"]
position = Vector2( 0.12373, 38 )

[node name="ImpactoSFX" parent="." index="3"]
stream = ExtResource( 12 )
volume_db = -4.727

[node name="Escudo" parent="." index="4" instance=ExtResource( 10 )]

[node name="EstelaPuntoInicio" parent="." index="5" instance=ExtResource( 6 )]
position = Vector2( -16, 0 )

[node name="MotorSFX" parent="." index="6" instance=ExtResource( 8 )]

[node name="AnimationPlayer" parent="." index="7" instance=ExtResource( 9 )]

[node name="CamaraPlayer" parent="." index="8" instance=ExtResource( 4 )]

[node name="LaserBeam2D" parent="." index="9" instance=ExtResource( 5 )]
position = Vector2( 30.8677, 0 )
cast_speed = 6000.0
max_length = 1200.0
growth_time = 0.3

[node name="BarraSalud" parent="." index="10"]
margin_left = -72.5613
margin_right = 39.4387
margin_bottom = 74.0

[connection signal="animation_finished" from="AnimationPlayer" to="." method="_on_AnimationPlayer_animation_finished"]

[editable path="Canion"]
